module(
    name = "bazel-example",
    version = "1.0.0",
)

load_conan_dependencies = use_extension("//conan_deps:conan_deps_module_extension.bzl", "conan_extension")
use_repo(
    load_conan_dependencies,
    "boost",
    "fmt",
)
